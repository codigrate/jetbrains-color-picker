# Version and since-build live in src/main/resources/META-INF/plugin.xml
# (hand-maintained, like the Codigrate theme plugins); the build reads them
# from there.

# Path to a locally installed JetBrains IDE used as the compile/run platform.
# When unset (e.g. CI or another machine), the build downloads IntelliJ IDEA
# Community instead - see build.gradle.kts.
localIdePath=/Users/furkanyavuz/Applications/IntelliJ IDEA.app

org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
